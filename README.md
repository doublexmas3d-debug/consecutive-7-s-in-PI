# Function to Count Frequency of Consecutive 7 in PI

The number PI is approximated as `3.14159...`. but actually PI is endless with absolutly no ending.An infinite number like this makes ypu think "what kind of unique and abnormal and fun sequences can we find ? "
The problem I recieved was ,How many consecutive sevens can get in pi and to what extent ? ,

I will make a table where it has x as the numbers  2-10000... and if inn Pi there is that of amount of 
consecutive sevens I output 1 however if there isnt it will output 0.

The maths for this is  : f(x) = {1,If x consecutive 7's exists in PI}
                                {0 , Otherwise}

 example >>> pi = 2,5,7,7,7,4,5 : f(3) = 1 (becuase there are 3 consecutive 7's)      



This program will be coded in pyhton at frist but will later be coded into csharp